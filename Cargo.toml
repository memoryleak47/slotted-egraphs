[package]
name = "slotted-egraphs"
version = "0.0.2"
edition = "2021"
description = "E-Graphs with name binding"
license = "Apache-2.0 OR MIT"
repository = "https://github.com/memoryleak47/egraph-sandbox/"

[features]
explanations = []
checks = []

[dependencies]
fnv = "1.0.7"
lazy_static = "1.5.0"

[dev-dependencies]
symbol_table = { version = "0.3", features = ["global"]}
rand = "0.8.5"

[profile.release]
debug = true
